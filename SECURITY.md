# 小黄提醒管家安全说明

## 本地优先

小黄提醒管家不提供开发者自建账户服务器。未启用 iCloud 时，记录只保存在当前设备；启用 iCloud 时，应用只上传由同步密码加密后的密文。

## 本地保险箱

- 每个保险箱生成独立的 256 位随机数据密钥，记录使用 AES-256-GCM 加密。
- 本地密码通过 PBKDF2-SHA256（310,000 次迭代）生成包裹密钥；不会保存明文密码。
- Touch ID / Face ID 快速解锁使用系统认证界面，设备数据密钥保存在 Apple Keychain。
- 关闭本地保险箱后，资料会回到普通本地存储；共享设备不建议关闭保险箱。

## 备份与互传

- 加密文件和邮箱文本备份使用独立备份密码与 AES-256-GCM。
- 密码、API Key、Token、Secret、私钥字段默认排除，只有用户明确选择时才包含。
- 二维码不承载真实记录，只承载同一局域网的一次性地址、随机令牌与临时解密钥匙；发送端五分钟过期，成功下载一次即停止。
- 导入和同步按记录 ID 与更新时间合并；删除使用墓碑标记，避免旧设备把已删除记录恢复出来。

## 恢复边界

小黄提醒管家没有服务器管理员，也没有密码重置后门。忘记本地密码或备份密码时，开发者无法恢复密文。请至少保留一份可验证的加密文件备份，并把备份密码保存在与备份文件不同的位置。

## iCloud 限制

iCloud 同步使用 Apple `NSUbiquitousKeyValueStore`，适用于文本资料，单个加密同步包限制为 800 KB。正式设备同步需要有效的 Apple Developer 签名与 iCloud capability；未配置时仍可正常使用本地保险箱、二维码互传和文件备份。
